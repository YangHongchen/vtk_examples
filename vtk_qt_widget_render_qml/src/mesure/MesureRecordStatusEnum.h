#ifndef MESURE_RECORD_STATUS_ENUM_H
#define MESURE_RECORD_STATUS_ENUM_H

// 检测记录状态枚举
enum class MesureRecordStatusEnum
{
    // 定义枚举值（显示赋值）
    Pending     = 0,
    Testing     = 1,
    Completed   = 2,
    Cancelled   = 3
};

#endif // MESURE_RECORD_STATUS_ENUM_H
